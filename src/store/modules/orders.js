const state = {
    orders : [
        
    ]
}

const getters = {
    allOrders: (state) => state.orders
}

const actions = {}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}